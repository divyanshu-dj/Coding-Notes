#include <bits/stdc++.h>
using namespace std;

// structure to define a node of the doubly linked list
class node{
    int data;
    node *prev;
    node *next;

    node(int data){
      this-> data = data;
        
    }
};
